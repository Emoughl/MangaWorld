<div class="list-group">
    @foreach (var tinhtrang in Model)
    {
        <a href="@Url.Action("Truyentheotinhtrang","Body", new {id = tinhtrang.MaTinhTrang})" class="list-group-item"> @tinhtrang.TenTinhTrang</a>

    }
</div>

